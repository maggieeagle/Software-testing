// I decided to do the course using JS and JEST testing framework. 
// I use JS (actually TS + React) in my other projects and believe it will be helpful.

function hello(name) {
    return `Hello, ${name}`
}
module.exports = hello;